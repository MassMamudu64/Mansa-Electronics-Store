# =============================================================================
# Mansa Electronics — environment template
# Copy to .env.local and fill in real values. .env.local is gitignored.
#
# MIGRATION NOTE:
# Hostinger MySQL configuration preserved below as comments (DEPRECATED).
# Supabase PostgreSQL configuration is now active.
# =============================================================================

# ─── Database (REQUIRED) ─────────────────────────────────────────────────────
#
# DEPRECATED — Hostinger MySQL connection (kept for reference only)
# In hPanel → Databases → Remote MySQL, whitelist the IP of your dev/prod
# host, otherwise the connection fails with "Host not allowed".
# DATABASE_URL="mysql://USER:PASSWORD@auth-db1864.hstgr.io:3306/u683702108_mansa_buy"
#
# ACTIVE — Supabase PostgreSQL connection (pooled, port 6543, PgBouncer)
# Use the *Transaction* pooler URL for the runtime client. Get it from
# Supabase Dashboard → Project Settings → Database → Connection string →
# "Transaction pooler". `pgbouncer=true` and `connection_limit=1` are
# r
# TODO: paste the real password from Supabase (URL-encode any special chars).
DATABASE_URL="postgresql:postgresql://postgres:MansaMass@@2026@db.jyqkfvrqmjkrxjrypkwa.supabase.co:5432/postgres"

# ACTIVE — Direct (non-pooled) connection on port 5432.
# REQUIRED by `prisma migrate` / `prisma db push` (DDL cannot run through
# PgBouncer transaction mode). Used only at migration time, never at runtime.
# TODO: paste the same password as above.
DIRECT_URL="postgresql://postgres:MansaMass@@2026@db.jyqkfvrqmjkrxjrypkwa.supabase.co:5432/postgres"

# ─── Supabase project keys (REQUIRED for client + admin SDK usage) ───────────
# Dashboard → Project Settings → API
# TODO: fill in from Supabase dashboard.
SUPABASE_URL=https://jyqkfvrqmjkrxjrypkwa.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp5cWtmdnJxbWprcnhqcnlwa3dhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzgyOTkxNjgsImV4cCI6MjA5Mzg3NTE2OH0.86Pq3wxc5xRfyCqQnt4_tJWonU3reeMidRtnJbi5kcE
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp5cWtmdnJxbWprcnhqcnlwa3dhIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3ODI5OTE2OCwiZXhwIjoyMDkzODc1MTY4fQ.1n6Xw6g-paXp09c79MNt7mNNVYnv2y45WfRMJpii3Mg
SUPABASE_DB_URL=

# Browser-exposed copies (only the URL + anon key — NEVER the service role)
NEXT_PUBLIC_SUPABASE_URL=https://jyqkfvrqmjkrxjrypkwa.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp5cWtmdnJxbWprcnhqcnlwa3dhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzgyOTkxNjgsImV4cCI6MjA5Mzg3NTE2OH0.86Pq3wxc5xRfyCqQnt4_tJWonU3reeMidRtnJbi5kcE

# ─── Admin authentication (REQUIRED) ─────────────────────────────────────────
# Custom HMAC-cookie session — NOT migrated to Supabase Auth.
# Pair credentials. Add ADMIN_ID_2 / ADMIN_PASSWORD_2, _3, … for more admins.
ADMIN_ID=0011
ADMIN_PASSWORD=MansaMass@@2026

# Secret used to sign session cookies (HMAC-SHA256). >= 32 chars. Generate:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
ADMIN_SESSION_SECRET=replace-me-with-at-least-32-random-bytes-base64url

# ─── Inventory read API key (OPTIONAL) ───────────────────────────────────────
# Send as `x-api-key:` or `Authorization: Bearer`. >= 16 chars to be accepted.
INVENTORY_API_KEY=

# ─── SMTP for order email receipts (OPTIONAL) ────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=masskeita64@gmail.com
SMTP_PASS=your_app_password

MAIL_FROM="Mansa Electronics <orders@mansa-electronics.com>"
ORDERS_INBOX=owner@mansa-electronics.com

# Public storefront URL (used in email body links + auth callbacks)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
